# 标签数量变化时的微调流程说明

## 📋 问题解答

### Q: 标签数量变化后，是否需要手动创建新的分类头？

**答案：不需要！** 代码已经自动处理了这个问题。

## 🔄 完整流程解析

### 1. 为什么需要替换分类头？

```
原始 iNaturalist 模型：
├── 骨干网络（特征提取器）
│   └── 学习到的通用植物特征（可复用）
└── 分类头（输出层）
    └── 10000 个类别的输出（需要替换）

你的自定义数据集：
├── 3 个类别（或其他数量）
└── 需要 3 个类别的输出
```

**关键点**：
- 骨干网络学到的特征可以复用（植物识别的基础特征）
- 分类头必须匹配你的类别数（3类 vs 10000类）

### 2. 自动处理流程（代码已实现）

当你运行训练脚本时，`create_model()` 函数会自动执行以下步骤：

#### 步骤 1: 加载预训练模型（原始 10000 类）
```python
pretrained_model = timm.create_model(
    'hf_hub:timm/eva02_large_patch14_clip_336.merged2b_ft_inat21',
    pretrained=True,  # 加载预训练权重
)
# 此时模型有 10000 个输出类别
```

#### 步骤 2: 创建新模型（你的类别数，如 3 类）
```python
model = timm.create_model(
    'hf_hub:timm/eva02_large_patch14_clip_336.merged2b_ft_inat21',
    pretrained=False,  # 不自动加载权重
    num_classes=3,  # 你的类别数
)
# timm 自动创建新的分类头，输出维度为 3
```

**重要**：`timm.create_model()` 会自动创建分类头，你不需要手动编写！

#### 步骤 3: 复制预训练权重（跳过分类头）
```python
# 从预训练模型复制权重
for name, param in pretrained_model.state_dict().items():
    if 'head.' in name or 'fc.' in name or 'classifier.' in name:
        # 跳过分类头，不复制
        continue
    else:
        # 复制骨干网络的权重
        model_state[name] = param

model.load_state_dict(model_state, strict=False)
```

**结果**：
- ✅ 骨干网络：使用预训练权重（已学习植物特征）
- ✅ 分类头：随机初始化（等待训练）

#### 步骤 4: 开始训练
```python
# 训练时，只有分类头（或整个模型）会更新
optimizer = optim.AdamW(model.parameters(), lr=0.0001)
```

## 📊 可视化流程

```
┌─────────────────────────────────────────────────────────┐
│  步骤 1: 加载 iNaturalist 预训练模型                    │
│  ┌─────────────────────────────────────┐              │
│  │ 骨干网络 (预训练权重)                │              │
│  │  └─> 特征提取器                      │              │
│  │                                      │              │
│  │ 分类头 (10000 类)                    │              │
│  │  └─> [10000 维输出]                  │              │
│  └─────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  步骤 2: 创建新模型（你的类别数）                        │
│  ┌─────────────────────────────────────┐              │
│  │ 骨干网络 (未初始化)                  │              │
│  │  └─> 特征提取器                      │              │
│  │                                      │              │
│  │ 分类头 (3 类) ← timm 自动创建！      │              │
│  │  └─> [3 维输出]                      │              │
│  └─────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  步骤 3: 复制预训练权重                                  │
│  ┌─────────────────────────────────────┐              │
│  │ 骨干网络 (✅ 复制预训练权重)         │              │
│  │  └─> 特征提取器 (已学习植物特征)     │              │
│  │                                      │              │
│  │ 分类头 (❌ 跳过，保持随机初始化)      │              │
│  │  └─> [3 维输出] (等待训练)           │              │
│  └─────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│  步骤 4: 开始训练                                        │
│  ┌─────────────────────────────────────┐              │
│  │ 骨干网络 (可选：冻结或微调)          │              │
│  │  └─> 特征提取器                      │              │
│  │                                      │              │
│  │ 分类头 (✅ 训练中...)                 │              │
│  │  └─> [3 维输出] (学习你的类别)       │              │
│  └─────────────────────────────────────┘              │
└─────────────────────────────────────────────────────────┘
```

## 🎯 三种训练策略

### 策略 1: 只训练分类头（推荐用于小数据集）

```bash
python train.py \
    --dataset dataset \
    --model hf_hub:timm/eva02_large_patch14_clip_336.merged2b_ft_inat21 \
    --freeze-backbone \
    --epochs 20 \
    --lr 0.01 \
    --use-inat-norm
```

**流程**：
1. 骨干网络：冻结（不更新权重）
2. 分类头：训练（学习你的 3 个类别）

**优点**：
- 训练快
- 显存占用少
- 适合小数据集（< 1000 样本）

### 策略 2: 全模型微调（推荐用于中等/大数据集）

```bash
python train.py \
    --dataset dataset \
    --model hf_hub:timm/eva02_large_patch14_clip_336.merged2b_ft_inat21 \
    --epochs 50 \
    --batch-size 16 \
    --lr 0.0001 \
    --use-inat-norm
```

**流程**：
1. 骨干网络：微调（在预训练基础上继续学习）
2. 分类头：训练（学习你的 3 个类别）

**优点**：
- 性能通常更好
- 能适应你的数据分布

**缺点**：
- 需要更多计算资源
- 训练时间更长

### 策略 3: 渐进式解冻（高级策略）

```bash
# 第一步：只训练分类头
python train.py --freeze-backbone --epochs 10 --lr 0.01

# 第二步：解冻最后几层
python train.py --freeze-layers 10 --epochs 10 --lr 0.001

# 第三步：全模型微调
python train.py --epochs 30 --lr 0.0001
```

## 🔍 代码中的关键部分

### 自动创建分类头（第 194-201 行）

```python
# 创建新模型（替换分类头）
print(f"  创建新模型（替换分类头为 {num_classes} 类）...")
model = timm.create_model(
    model_name,
    pretrained=False,  # 不使用自动加载，手动加载权重
    num_classes=num_classes,  # ← 这里指定你的类别数
    drop_rate=drop_rate,
    drop_path_rate=drop_path_rate,
)
# timm 会自动创建匹配 num_classes 的分类头！
```

### 跳过分类头复制权重（第 216-222 行）

```python
for name, param in pretrained_state.items():
    # 跳过分类头相关的层
    is_classifier = any(keyword in name for keyword in 
                       ['head.', 'fc.', 'classifier.', 'head.fc.', 'head.classifier.'])
    
    if is_classifier:
        skipped_layers += 1
        continue  # ← 跳过分类头，不复制
    
    # 复制骨干网络权重
    if name in model_state and model_state[name].shape == param.shape:
        model_state[name] = param
        loaded_layers += 1
```

## ✅ 总结

1. **不需要手动创建分类头**：`timm.create_model(num_classes=3)` 会自动创建
2. **不需要手动替换分类头**：代码会自动跳过分类头，只复制骨干网络权重
3. **分类头会自动初始化**：PyTorch 默认使用随机初始化
4. **你只需要**：
   - 准备数据集（按类别文件夹组织）
   - 运行训练命令
   - 代码会自动处理一切！

## 🚀 快速开始

```bash
# 1. 准备数据集（3 个类别文件夹）
dataset/
├── 0_0656_tree1/
├── 1_0654_tree2/
└── 2_8877_tree3/

# 2. 运行训练（代码会自动处理分类头替换）
python train.py \
    --dataset dataset \
    --model hf_hub:timm/eva02_large_patch14_clip_336.merged2b_ft_inat21 \
    --epochs 50 \
    --batch-size 16 \
    --lr 0.0001 \
    --use-inat-norm

# 3. 训练过程中会看到：
#   创建模型: hf_hub:timm/eva02_large_patch14_clip_336.merged2b_ft_inat21
#     - 自定义类别数: 3
#     加载预训练模型（原始类别数）...
#     创建新模型（替换分类头为 3 类）...
#     加载预训练权重（跳过分类头）...
#     权重加载完成:
#       - 已加载: XXX 层
#       - 已跳过: X 层（分类头或不存在）
```

## 💡 常见问题

### Q: 如果我的类别数从 3 变成 5，需要重新训练吗？

**A**: 是的，需要重新训练。因为分类头的输出维度变了（3 → 5），需要：
1. 重新创建模型（`num_classes=5`）
2. 重新训练分类头（学习新的 5 个类别）

### Q: 可以复用之前训练的骨干网络权重吗？

**A**: 可以！你可以：
1. 加载之前训练的检查点
2. 只提取骨干网络部分
3. 创建新的分类头（新的类别数）
4. 继续训练

### Q: 分类头是如何初始化的？

**A**: PyTorch 默认使用随机初始化（通常是 Kaiming 或 Xavier 初始化）。这是合理的，因为：
- 分类头需要学习你的特定类别
- 随机初始化让模型从头学习你的类别映射

### Q: 为什么不能直接使用原来的 10000 类分类头？

**A**: 因为：
1. 输出维度不匹配（10000 vs 3）
2. 原来的分类头学习的是 iNaturalist 的 10000 个类别
3. 你的数据集只有 3 个类别，需要重新学习

---

**结论**：你不需要手动创建分类头，代码已经自动处理了所有步骤！只需要准备数据集并运行训练命令即可。
